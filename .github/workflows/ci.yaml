name: 🏗️ CI

# Responsibility: Required verification for pull requests and merge queue
# - Install dependencies with pnpm
# - Run the repository quality gate
# - Build the production app with release metadata
#
# Triggered on: Push/PR to master + merge queue

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  merge_group:
    types: [checks_requested]
  workflow_dispatch:

env:
  NODE_VERSION: "24"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: read

concurrency:
  group: verify-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: ✅ Verify
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6

      # pnpm/action-setup@v6 can mis-handle pnpm 10 lockfiles on GitHub Actions
      # and fail during install with ERR_PNPM_BROKEN_LOCKFILE even when the
      # repository lockfile is valid. Use v4 and let package.json's
      # `packageManager` field remain the single source of truth.
      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: 📦 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: ✅ Run quality gate and build
        run: pnpm build

      - name: 📊 Upload build artifacts
        uses: actions/upload-artifact@v7
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        with:
          name: build-${{ github.sha }}
          path: dist/
          if-no-files-found: error
          retention-days: 7
          compression-level: 9
