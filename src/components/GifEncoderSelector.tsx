/**
 * GifEncoderSelector (removed)
 *
 * The GIF encoder is now fully auto-selected inside the orchestrator.
 * This UI control was removed to keep the UX simple and prevent users from
 * choosing unstable combinations on mobile / low-memory devices.
 */
export {};
