/**
 * Strategy Resolver (deprecated)
 *
 * Retained for backward compatibility.
 * The canonical implementation lives in `@utils/conversion-strategy`.
 */

export type { ConversionStrategy } from "@utils/conversion-strategy";
export { getConversionStrategy } from "@utils/conversion-strategy";
